Download and install:
-Mozilla Firefox
-Notepadpp (Notepad++). Once downloaded, Plugins -> Plugins admin -> Search JSON viewer -> Install
-Node.js

Create two empty folders somewhere easy to find called:
clipped
games


**skip this for now** 1. Then in cmd/terminal navigate to your newly created games folder
**skip this for now** 2. Then run "npm install ws" (without the quotes) in that folder

We will share the game files on USB at first. May integrate github later (it's finnicky)
